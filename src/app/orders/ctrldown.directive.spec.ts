import { CtrldownDirective } from './ctrldown.directive';

describe('CtrldownDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrldownDirective();
    expect(directive).toBeTruthy();
  });
});
